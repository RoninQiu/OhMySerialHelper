# =====================
# Tauri / Rust
# =====================
# v1.3.0 起转为 cargo 工作区，构建产物落在仓库根 target/（src-tauri/target 是拆分前的遗留路径）
/target
src-tauri/target
src-tauri/gen

# =====================
# 运行 / 调试日志
# =====================
stdout.log
stderr.log

# =====================
# Frontend / Node
# =====================
dist
node_modules
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
*.tsbuildinfo

# =====================
# Tests / Coverage
# =====================
coverage
*.lcov

# =====================
# Environment
# =====================
.env
.env.local
.env.*.local

# =====================
# Editor / IDE
# =====================
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store
Thumbs.db
desktop.ini

# =====================
# Project-specific
# =====================
# Old project memory (replaced by AI 会话记忆文件)
PROJECT_MEMORY.md

# AI 协作者本地配置：会话记忆 / 规则 / skills / 工具白名单（一律不入 Git）
.agents/

# 历史遗留：Claude Code 时期的路径（已迁移到 .agents/，保留以防误加）
.claude/
CLAUDE.md

# AI/assistant-driven scripts (local-only dev helpers, e.g. release.sh)
# 规则：与项目实际产出无关的 AI 工具脚本一律不入 Git
scripts/

# codegraph
.codegraph/

# egui 实验版（eframe + egui，无 WebView2）— 本地保留、暂停中，不进版本控制
# 暂停原因非代码缺陷：缺 CJK 字体（中文会渲染成 ?）+ 本机存在按映像名杀进程的环境问题
egui-app/
